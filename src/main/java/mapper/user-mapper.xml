<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="userDao">
    <!-- type映射的类名，id后面设置resultMap时用到，表示结果对应哪个类型的映射关系 -->
    <!-- 注：如果表中列名与类的属性名一致，且类按javaBean规范构造，映射关系可省略，具体原因可参考java的反射原理 -->
    <resultMap type="User" id="UserMapper">
        <id column="user_id" property="userId"/>
        <result column="username" property="username" />
        <result column="phone" property="phone" />
        <result column="address" property="address" />
        <result column="remark" property="remark"/>
    </resultMap>
     
    <select id="selectAllUser" parameterType="map" resultMap="UserMapper">
        select * from user where username=#{username} and phone=#{phone};
    </select>
     

</mapper>